# Core dependencies for browser-agent framework
playwright>=1.47.0
typer>=0.12.0
rich>=13.0.0

# Development dependencies (optional)
# Install with: pip install -r requirements.txt -r requirements-dev.txt
